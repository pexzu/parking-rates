import styled from '@emotion/styled';

export const PrimaryButton = styled('button')`
  label: PrimaryButton;

  padding: 6px 15px;
  color: #ffffff;
  background: #224172;
  border-radius: 3px;
  border: 0;

  &:hover {
    opacity: 0.9;
  }
`;
